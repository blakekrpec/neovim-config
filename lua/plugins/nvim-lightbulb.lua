return {
    "kosayoda/nvim-lightbulb",
    opts = {
        autocmd = { enabled = true }, -- automatically update the lightbulb
        sign = { enabled = true },    -- show lightbulb in the sign column
        virtual_text = { enabled = false }, -- or enable virtual text instead
    },
}

