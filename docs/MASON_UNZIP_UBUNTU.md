#### **Mason not installing without unzip**

Mason requires `unzip` to install many packages on Ubuntu. If installations are failing, `:MasonLog` may report errors related to missing tools. Install `unzip` to resolve:

```bash
sudo apt install unzip
```

[Back to README](../README.md)
